document.addEventListener('DOMContentLoaded', () => {
    const toast = m => { const e = document.querySelector('#liveToast'); if (!e) return; e.querySelector('.toast-body').textContent = m; bootstrap.Toast.getOrCreateInstance(e).show() };
    document.querySelectorAll('[data-toast]').forEach(b => b.addEventListener('click', e => { e.preventDefault(); toast(b.dataset.toast || 'Done') }));
    document.querySelectorAll('.qty').forEach(g => { const i = g.querySelector('input'); g.querySelector('.minus')?.addEventListener('click', () => i.value = Math.max(1, (+i.value || 1) - 1)); g.querySelector('.plus')?.addEventListener('click', () => i.value = (+i.value || 1) + 1) });
    document.querySelectorAll('[data-password-toggle]').forEach(b => b.addEventListener('click', () => { const i = b.parentElement.querySelector('input'); i.type = i.type === 'password' ? 'text' : 'password'; b.querySelector('i').className = i.type === 'text' ? 'bi bi-eye-slash' : 'bi bi-eye' }));
    const p = document.querySelector('[data-register-password]'); p?.addEventListener('input', () => { const v = p.value, l = [v.length >= 8, /[A-Z]/.test(v), /\d/.test(v), /[^A-Za-z0-9]/.test(v)].filter(Boolean).length; document.querySelector('.password-strength').className = 'password-strength level-' + l; document.querySelector('[data-password-strength]').textContent = ['Use 8+ characters with a number', 'Fair — add uppercase', 'Good — add a symbol', 'Strong password', 'Excellent password'][l] });
    document.querySelectorAll('[data-account-form]').forEach(f => f.addEventListener('submit', e => { e.preventDefault(); if (!f.reportValidity()) return; const m = document.querySelector('[data-account-message]'); m.textContent = f.dataset.accountMode === 'login' ? 'Signed in successfully.' : 'Account created successfully.'; m.classList.add('show') }));
    document.querySelectorAll('[data-price-filter]').forEach(filter => {
        const minInput = filter.querySelector('[data-price-min]');
        const maxInput = filter.querySelector('[data-price-max]');
        const output = filter.querySelector('[data-price-output]');
        const progress = filter.querySelector('[data-price-progress]');
        const floor = Number(filter.dataset.min || minInput.min);
        const ceiling = Number(filter.dataset.max || maxInput.max);
        const minimumGap = Number(minInput.step || 1);
        const money = value => `৳${Number(value).toLocaleString('en-BD')}`;
        const render = changedInput => {
            let minValue = Number(minInput.value);
            let maxValue = Number(maxInput.value);
            if (maxValue - minValue < minimumGap) {
                if (changedInput === minInput) minValue = maxValue - minimumGap;
                else maxValue = minValue + minimumGap;
            }
            minInput.value = minValue;
            maxInput.value = maxValue;
            const range = ceiling - floor;
            progress.style.setProperty('--range-left', `${((minValue - floor) / range) * 100}%`);
            progress.style.setProperty('--range-right', `${100 - ((maxValue - floor) / range) * 100}%`);
            output.textContent = `${money(minValue)} – ${money(maxValue)}`;
        };
        minInput.addEventListener('input', () => render(minInput));
        maxInput.addEventListener('input', () => render(maxInput));
        filter.querySelector('[data-price-reset]')?.addEventListener('click', () => {
            minInput.value = floor;
            maxInput.value = ceiling;
            render();
        });
        render();
    });
    document.querySelectorAll('.product-action,.smart-wishlist,.more-product-heart,.shop-heart').forEach(b => b.addEventListener('click', e => { e.preventDefault(); b.classList.toggle('active'); const i = b.querySelector('.bi-heart,.bi-heart-fill'); if (i) i.className = b.classList.contains('active') ? 'bi bi-heart-fill' : 'bi bi-heart' }));
    document.querySelectorAll('.desktop-category-item>a').forEach(a => a.addEventListener('click', e => { if (innerWidth < 992) return; e.preventDefault(); const x = a.parentElement; document.querySelectorAll('.desktop-category-item').forEach(o => o !== x && o.classList.remove('open')); x.classList.toggle('open') }));
    document.querySelectorAll('[data-trending-filter]').forEach(t => t.addEventListener('click', () => { document.querySelectorAll('[data-trending-filter]').forEach(x => x.classList.remove('active')); t.classList.add('active'); document.querySelectorAll('[data-trending-group]').forEach(x => x.classList.toggle('trending-muted', t.dataset.trendingFilter !== 'all' && x.dataset.trendingGroup !== t.dataset.trendingFilter && x.dataset.trendingGroup !== 'all')) }));
    document.querySelector('[data-view-more-products]')?.addEventListener('click', e => { document.querySelectorAll('.more-product-hidden').forEach((x, i) => setTimeout(() => x.classList.remove('more-product-hidden'), i * 70)); e.currentTarget.closest('.more-products-action').querySelector('small').textContent = 'Showing all 15 products'; e.currentTarget.remove() });
    const timer = document.querySelector('[data-campaign-countdown]'); if (timer) setInterval(() => { const s = Math.max(0, (+timer.dataset.campaignCountdown || 0) - 1); timer.dataset.campaignCountdown = s;[Math.floor(s / 86400), Math.floor(s % 86400 / 3600), Math.floor(s % 3600 / 60), s % 60].forEach((v, i) => timer.querySelectorAll('b')[i].textContent = String(v).padStart(2, '0')) }, 1000);
    document.querySelectorAll('[data-countdown]').forEach(t => setInterval(() => { const s = Math.max(0, (+t.dataset.countdown || 0) - 1); t.dataset.countdown = s;[Math.floor(s / 3600), Math.floor(s % 3600 / 60), s % 60].forEach((v, i) => t.querySelectorAll('b span')[i].textContent = String(v).padStart(2, '0')) }, 1000));
    const deals = document.querySelector('[data-deals-track]'); if (deals) { const cards = [...deals.querySelectorAll('.deal-card')]; document.querySelector('.deal-prev')?.addEventListener('click', () => deals.scrollBy({ left: -240, behavior: 'smooth' })); document.querySelector('.deal-next')?.addEventListener('click', () => deals.scrollBy({ left: 240, behavior: 'smooth' })); document.querySelectorAll('[data-deal-filter]').forEach(t => t.addEventListener('click', () => { document.querySelectorAll('[data-deal-filter]').forEach(x => x.classList.remove('active')); t.classList.add('active'); cards.forEach(x => x.classList.toggle('is-hidden', t.dataset.dealFilter !== 'all' && x.dataset.dealCategory !== t.dataset.dealFilter)) })) }
    const main = document.querySelector('[data-product-main]'), zoom = document.querySelector('[data-zoom-container]'), modal = document.querySelector('[data-zoom-modal]'); if (main && zoom) { document.querySelectorAll('[data-product-image]').forEach(b => b.addEventListener('click', () => { document.querySelectorAll('[data-product-image]').forEach(x => x.classList.remove('active')); b.classList.add('active'); main.src = b.dataset.productImage; main.style.objectPosition = b.dataset.position || 'center'; modal.querySelector('[data-zoom-image]').src = b.dataset.productImage })); zoom.addEventListener('mousemove', e => { if (!matchMedia('(hover:hover)').matches) return; const r = zoom.getBoundingClientRect(); main.style.transformOrigin = ((e.clientX - r.left) / r.width * 100) + '% ' + ((e.clientY - r.top) / r.height * 100) + '%'; main.style.transform = 'scale(1.8)' }); zoom.addEventListener('mouseleave', () => main.style.transform = 'scale(1)'); document.querySelector('[data-open-zoom]')?.addEventListener('click', () => modal.classList.add('show')); document.querySelector('[data-close-zoom]')?.addEventListener('click', () => modal.classList.remove('show')); document.querySelectorAll('.color-swatches button').forEach(b => b.addEventListener('click', () => { document.querySelectorAll('.color-swatches button').forEach(x => x.classList.remove('active')); b.classList.add('active'); document.querySelector('[data-selected-color]').textContent = b.dataset.color })) }
    const wishlist = document.querySelector('[data-wishlist-items]'); if (wishlist) wishlist.addEventListener('click', e => { const item = e.target.closest('[data-wishlist-item]'); if (!item) return; if (e.target.closest('[data-wishlist-remove],[data-wishlist-remove-mobile]')) { item.classList.add('removing'); setTimeout(() => item.remove(), 250) } if (e.target.closest('[data-wishlist-cart]')) toast('Added to cart') });
    const cart = document.querySelector('[data-cart-items]'); if (cart) { const money = v => '৳' + v.toLocaleString(), update = () => { let s = 0; document.querySelectorAll('[data-cart-item]').forEach(x => { const q = Math.max(1, +x.querySelector('input').value || 1), v = +x.dataset.price * q; x.querySelector('input').value = q; x.querySelector('.cart-line-total').textContent = money(v); s += v }); document.querySelector('[data-cart-subtotal]').textContent = money(s); document.querySelector('[data-cart-total]').textContent = money(s + (+(document.querySelector('input[name="shipping"]:checked')?.value || 0))) }; cart.addEventListener('click', e => { const x = e.target.closest('[data-cart-item]'); if (x && e.target.closest('[data-remove-item],[data-move-wishlist]')) { x.remove(); update() } if (e.target.closest('.minus,.plus')) setTimeout(update) }); document.querySelectorAll('input[name="shipping"]').forEach(x => x.addEventListener('change', update)); update() }
    const checkout = document.querySelector('[data-checkout-form]'); if (checkout) { const update = () => { const s = +(document.querySelector('input[name="checkoutShipping"]:checked')?.value || 0), v = 19230 + s; document.querySelector('[data-checkout-shipping]').textContent = s ? '৳' + s : 'Free'; document.querySelector('[data-checkout-total]').textContent = '৳' + v.toLocaleString(); document.querySelector('[data-checkout-mobile-total]').textContent = '৳' + v.toLocaleString() }; document.querySelectorAll('.checkout-options>label>input').forEach(x => x.addEventListener('change', () => { document.querySelectorAll('input[name="' + x.name + '"]').forEach(y => y.closest('label').classList.toggle('selected', y.checked)); if (x.name === 'payment') document.querySelector('.card-fields').style.display = x.value === 'card' ? 'grid' : 'none'; update() })); checkout.addEventListener('submit', e => { e.preventDefault(); if (checkout.reportValidity()) document.querySelector('[data-checkout-success]').classList.add('show') }); document.querySelector('[data-place-order]')?.addEventListener('click', () => checkout.requestSubmit()); update() }
});
